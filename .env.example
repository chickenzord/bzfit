# BzFit Environment Variables
# Copy this file to .env and fill in the values.
# For Docker deployments, set these as environment variables in your compose file or container.

# =============================================================================
# DATABASE
# =============================================================================

# SQLite (default for single-node / low-traffic deployments)
DATABASE_URL=file:./data/db.sqlite

# PostgreSQL (recommended for production)
# DATABASE_URL=postgresql://bzfit:yourpassword@localhost:5432/bzfit

# =============================================================================
# SERVER
# =============================================================================

# Port the server listens on (default: 3000 in Docker, 3001 in dev)
PORT=3001
NODE_ENV=development

# =============================================================================
# AUTHENTICATION
# =============================================================================

# Secret key used to sign JWT tokens.
# REQUIRED in production — use a long, random string (e.g. openssl rand -hex 32)
JWT_SECRET=change-me-in-production

# How long JWT access tokens are valid (default: 7d)
# Examples: 1d, 12h, 30m
JWT_EXPIRES_IN=7d

# =============================================================================
# SINGLE-USER MODE
# =============================================================================

# Allow new user registrations (default: true).
# Set to false for single-user / private self-hosted instances.
# Existing users can still log in; only new signups are blocked.
# REGISTRATION_ENABLED=false

# =============================================================================
# WEB APP (Docker build-time only)
# =============================================================================

# Base URL of the API server, baked into the Expo web app at Docker build time.
# Leave empty (default) when server and web app run in the same container —
# the web app will use relative URLs (/api/v1) which always point to the same host.
#
# Override only if you serve the web app separately from the API:
#   docker build --build-arg EXPO_PUBLIC_API_URL=https://api.example.com .
#
# EXPO_PUBLIC_API_URL=
